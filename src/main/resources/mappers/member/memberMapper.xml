<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.test.user.IMemberDAO">
<insert id="memberProc" parameterType="com.sp.test.user.MemberDTO">
	insert into mvcquiz2 values(#{id},#{pwd},#{name},#{email})
</insert>
</mapper>